apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: octobot-claim0
  name: octobot-claim0
  namespace: crypto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
