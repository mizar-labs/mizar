apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: searxng-claim0
  name: searxng-claim0
  namespace: searxng
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100G
